import './globals.css'; import type { Metadata } from 'next'; import { Inter } from 'next/font/google'; import { AuthProvider } from '@/context/authContext'; import Link from 'next/link'; import { useAuth } from '@/context/authContext'; const inter = Inter({ subsets: ['latin'] }); export const metadata: Metadata = { title: 'TaskMaster SaaS', description: 'Your ultimate task and project management solution.', }; const Header = () => { const { user, logout, loading } = useAuth(); return ( <header className="bg-dark text-white p-4 shadow-md"> <nav className="container mx-auto flex justify-between items-center"> <Link href={user ? "/dashboard" : "/"} className="text-2xl font-bold text-accent"> TaskMaster </Link> <ul className="flex space-x-4 items-center"> {loading ? ( <li className="text-gray-400">Loading...</li> ) : user ? ( <> <li> <Link href="/dashboard" className="hover:text-primary transition-colors"> Dashboard </Link> </li> <li> <Link href="/projects" className="hover:text-primary transition-colors"> Projects </Link> </li> <li className="text-gray-300">Welcome, {user.email}!</li> <li> <button onClick={logout} className="bg-red-500 hover:bg-red-600 text-white px-3 py-1 rounded-md transition-colors"> Logout </button> </li> </> ) : ( <> <li> <Link href="/login" className="hover:text-primary transition-colors"> Login </Link> </li> <li> <Link href="/signup" className="bg-primary hover:bg-primary/90 text-white px-3 py-1 rounded-md transition-colors"> Sign Up </Link> </li> </> )} </ul> </nav> </header> ); }; export default function RootLayout({ children }: { children: React.ReactNode; }) { return ( <html lang="en"> <body className={inter.className + " bg-gray-50 text-gray-900"}> <AuthProvider> <Header /> <main> {children} </main> </AuthProvider> </body> </html> ); } 