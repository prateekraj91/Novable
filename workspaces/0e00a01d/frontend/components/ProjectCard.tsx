import React from 'react'; import Link from 'next/link'; import { Project } from '@/lib/api'; import Button from './ui/Button'; interface ProjectCardProps { project: Project; onDelete: (projectId: string) => void; } const ProjectCard: React.FC<ProjectCardProps> = ({ project, onDelete }) => { return ( <div className="bg-white shadow rounded-lg p-4 flex flex-col justify-between"> <Link href={`/projects/${project.id}`} className="block"> <h3 className="text-lg font-semibold text-gray-900 hover:text-primary transition-colors mb-2"> {project.name} </h3> <p className="text-sm text-gray-600 mb-4 line-clamp-2"> {project.description || 'No description provided.'} </p> </Link> <div className="flex justify-end space-x-2"> <Link href={`/projects/${project.id}`} passHref> <Button variant="secondary" size="sm"> View </Button> </Link> <Button variant="danger" size="sm" onClick={() => onDelete(project.id)}> Delete </Button> </div> </div> ); }; export default ProjectCard; 