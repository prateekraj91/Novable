"use client";import React from 'react';import Link from 'next/link';import { useAuth } from '@/hooks/useAuth';import { useRouter } from 'next/navigation';export const Navbar: React.FC = () => {  const { user, logout, isAuthenticated } = useAuth();  const router = useRouter();  const handleLogout = () => {    logout();    router.push('/login');  };  if (!isAuthenticated) return null;  return (    <nav className="bg-primary text-white p-4 shadow-custom-medium">      <div className="container mx-auto flex justify-between items-center">        <Link href="/dashboard" className="text-2xl font-bold">          TaskMaster SaaS        </Link>        <div className="flex items-center space-x-4">          {user && <span className="text-sm hidden md:block">Welcome, {user.email}</span>}          <Link href="/dashboard/projects" className="hover:text-secondary">            Projects          </Link>          <Link href="/dashboard/tasks" className="hover:text-secondary">            Tasks          </Link>          <button            onClick={handleLogout}            className="bg-secondary hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded transition duration-200"          >            Logout          </button>        </div>      </div>    </nav>  );};