import React from 'react';interface Column<T> {  key: keyof T | 'actions';  header: string;  render?: (item: T) => React.ReactNode;}interface DataTableProps<T> {  data: T[];  columns: Column<T>[];  onEdit?: (item: T) => void;  onDelete?: (item: T) => void;  loading?: boolean;}export function DataTable<T extends { id: string }>(  { data, columns, onEdit, onDelete, loading }: DataTableProps<T>) {  if (loading) {    return (      <div className="flex justify-center items-center p-8 text-gray-600">        <svg className="animate-spin h-5 w-5 mr-3 text-primary" viewBox="0 0 24 24">          <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4"></circle>          <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>        </svg>        Loading data...      </div>    );  }  if (!data || data.length === 0) {    return <p className="p-4 text-center text-gray-600">No data available.</p>;  }  return (    <div className="overflow-x-auto relative shadow-custom-medium sm:rounded-lg">      <table className="w-full text-sm text-left text-gray-500">        <thead className="text-xs text-gray-700 uppercase bg-gray-50">          <tr>            {columns.map((column) => (              <th key={String(column.key)} scope="col" className="py-3 px-6">                {column.header}              </th>            ))}          </tr>        </thead>        <tbody>          {data.map((item) => (            <tr key={item.id} className="bg-white border-b hover:bg-gray-50">              {columns.map((column) => (                <td key={`${item.id}-${String(column.key)}`} className="py-4 px-6">                  {column.key === 'actions' ? (                    <div className="flex space-x-2">                      {onEdit && (                        <button                          onClick={() => onEdit(item)}                          className="font-medium text-primary hover:underline"                        >                          Edit                        </button>                      )}                      {onDelete && (                        <button                          onClick={() => onDelete(item)}                          className="font-medium text-red-600 hover:underline"                        >                          Delete                        </button>                      )}                    </div>                  ) : (                    column.render ? column.render(item) : String(item[column.key as keyof T])                  )}                </td>              ))}            </tr>          ))}        </tbody>      </table>    </div>  );}